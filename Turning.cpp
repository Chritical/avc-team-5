#include <stdio.h>
#include <time.h>
#include "E101.h"

int main() {

	init();
	}
	
	while (error >0){
		set_motor(1,64);
		set_motor(2,-64);
	}
	while (error < 0){
		set_motor(1,-64);
		set_motor(2,64);
	}
	while (error == 0){
		set_motor(1,64):
		set_motor(2,64);
	}
	
return0;}
