#include <stdio.h>
#include <time.h>
#include "E101.h"

int main() {

	init();
	}
	
	while (parameter == 1){
		set_motor(1,64);
		set_motor(2,-64);
	}
	while (parameter == 2){
		set_motor(1,-64);
		set_motor(2,64);
	}
	while (parameter == 0){
		set_motor(1,64):
		set_motor(2,64);
	}
	
return0;}
